import React from 'react';
import './Header.css'; // Importa solo il CSS di Navbar

function Header() {
  return (
    <header style={{ padding: '20px 0', textAlign: 'center', fontSize: '30px', fontWeight: 'bold' }}>
      🚗 Gestione Parcheggio
    </header>
  );
}

export default Header;
